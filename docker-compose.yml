services:
  trading-studio:
    image: ghcr.io/canpok1/trading-studio:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      TZ: Asia/Tokyo
    # 実取引が無いうちは自宅 LAN 内へそのまま公開する（ADR 0004）
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
