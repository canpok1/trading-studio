{
	"extends": "./tsconfig.base.json",
	"compilerOptions": {
		// 全パッケージとテストをまとめて検査する。環境の制限は各パッケージの tsconfig でかける
		"lib": ["ESNext", "DOM", "DOM.Iterable"],
		"jsx": "react-jsx",
		"types": ["bun"]
	},
	"include": ["packages/*/src", "packages/*/vite.config.ts"]
}
